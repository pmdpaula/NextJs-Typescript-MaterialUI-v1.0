query menuItemByName($name: String!) {
  menuItem(name: $name) {
    ident,
    name,
    link,
    Icon,
    access,
    disabled,
    items {
      ident,
      name,
      link,
      Icon,
      access,
      disabled,
  }
}}
